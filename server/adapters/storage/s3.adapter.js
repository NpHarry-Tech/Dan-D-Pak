import { notImplemented } from '../../core/http.js';

export function createS3Adapter() {
  return notImplemented('S3-compatible storage adapter is planned for VPS/cloud storage and is not wired yet.');
}
