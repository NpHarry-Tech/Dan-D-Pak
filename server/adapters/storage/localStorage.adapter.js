import { notImplemented } from '../../core/http.js';

export function createLocalStorageAdapter() {
  return notImplemented('Local storage adapter scaffold exists; current archive writes are still handled by server/services/archive.js.');
}
