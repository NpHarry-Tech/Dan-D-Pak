import { notImplemented } from '../../core/http.js';

export function createPostgresAdapter() {
  return notImplemented('PostgreSQL adapter scaffold exists, but the live app still uses SQLite until migrations and repositories are implemented.');
}
