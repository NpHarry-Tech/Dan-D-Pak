import { notImplemented } from '../../core/http.js';

export function createSupabaseAdapter() {
  return notImplemented('Supabase adapter scaffold exists for the temporary demo path; service-role access must stay backend-only.');
}
