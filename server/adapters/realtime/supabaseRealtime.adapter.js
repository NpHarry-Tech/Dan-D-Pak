import { notImplemented } from '../../core/http.js';

export function createSupabaseRealtimeAdapter() {
  return notImplemented('Supabase Realtime adapter is planned for temporary demo mode and is not wired yet.');
}
