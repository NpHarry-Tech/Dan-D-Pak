org.gradle.jvmargs=-Xmx2G -XX:MaxMetaspaceSize=768m -XX:ReservedCodeCacheSize=192m -XX:+HeapDumpOnOutOfMemoryError
org.gradle.workers.max=1
org.gradle.parallel=false
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# QA-TEMP: work around Kotlin build-tools-api incremental cache failure when
# pub-cache (C:) and project (D:) are on different drive roots on Windows.
# Revert after this QA run — see 02_APK_BUILD_AND_SIGNATURE.md.
kotlin.incremental=false
